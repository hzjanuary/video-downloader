# Frontend

Next.js App Router frontend for the video downloader platform setup slice.

## Commands

```bash
npm run dev
npm run build
npm run typecheck
```

The dev server listens on `http://localhost:3000` and reads the backend from
`NEXT_PUBLIC_API_BASE_URL`, defaulting to `http://localhost:8080`.
